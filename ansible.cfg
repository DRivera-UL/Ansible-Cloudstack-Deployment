[defaults]
remote_user = dante-rivera
inventory = ./inventory

[privilege_escalation]
# Ubuntu 25.10+ points /usr/bin/sudo at sudo-rs, whose password prompt Ansible
# cannot parse. Use the classic sudo binary that is still installed alongside it.
become_exe = /usr/bin/sudo.ws
